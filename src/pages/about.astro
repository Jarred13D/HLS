---
import { getCollection } from "astro:content";
import { Picture } from "astro:assets";
import Layout from "@/layouts/Layout.astro";
import Container from "@/components/container.astro";
import Sectionhead from "@/components/sectionhead.astro";

// Filter team entries with 'draft: false' & date before current date
const publishedTeamMembers = await getCollection("team", ({ data }) => {
  return !data.draft && data.publishDate < new Date();
});
---

<Layout title="About">
  <Container>
    <div class="flex flex-col gap-3 mx-auto max-w-4xl mt-16">
      <h2 class="font-bold text-3xl text-white justify-center text-center">
        About <span class="text-green-600">Me</span>
      </h2>

      <p class="text-lg leading-relaxed text-gray-400">
        My name is Nikolas Hampton and I’m the proud owner of <strong
          >Hampton Lawn Solutions</strong
        >.<br />
        <br />
        I first started cutting lawns as a kid, mowing neighbors’ yards while they
        were away on vacation. I quickly fell in love with the transformation — seeing
        an overgrown yard turn clean, sharp, and full of life sparked a passion that
        never left me.<br />
        <br />
        I went on to study business and play collegiate football, but after a series
        of injuries, I made the decision to fully commit to my dream: building my
        own lawn care company.<br />
        <br />
        At Hampton Lawn Solutions, my mission is simple — to show up on time, work
        with integrity, and make your yard look its absolute best. Whether you need
        a simple mow or a full-service landscaping plan, I’m here to help bring your
        vision to life.<br />
        <br />
        Let’s turn your yard into an oasis.
      </p>
    </div>

    <div class="flex flex-col gap-3 mx-auto max-w-4xl mt-16">
      <h2 class="font-bold text-3xl text-white justify-center text-center">
        About Hampton <span class="text-green-600">Lawn Solutions</span>
      </h2>
      <p class="text-lg leading-relaxed text-gray-400">
        At Hampton Lawn Solutions, we take pride in bringing out the beauty in
        every yard. As a small, locally owned business based in Benton Harbor,
        MI, we believe in providing reliable, professional and affordable
        lawncare tailored to each client’s unique needs. Whether it’s a simple
        mow or a full landscaping project, we approach every job with attention
        to detail and a passion for perfection.
      </p>
    </div>
    <!-- <div class="grid md:grid-cols-3 gap-10 mx-auto max-w-4xl mt-12">
      {
        publishedTeamMembers.map((teamMemberEntry) => (
          <div class="group">
            <div class="w-full aspect-square">
              <Picture
                src={teamMemberEntry.data.avatar.src}
                alt={teamMemberEntry.data.avatar.alt}
                sizes="(max-width: 800px) 100vw, 400px"
                width={400}
                height={400}
                class="w-full rounded-sm transition group-hover:-translate-y-1 group-hover:shadow-xl bg-white object-cover object-center aspect-square"
              />
            </div>

            <div class="mt-4 text-center">
              <h2 class="text-lg text-gray-800">{teamMemberEntry.data.name}</h2>
              <h3 class="text-sm text-slate-500">
                {teamMemberEntry.data.title}
              </h3>
            </div>
          </div>
        ))
      }
    </div> -->
  </Container>
</Layout>
